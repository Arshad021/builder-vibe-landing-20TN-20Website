{
  "version": 2,
  "buildCommand": "pnpm approve-builds && pnpm run build:client",
  "installCommand": "pnpm approve-builds && pnpm install",
  "outputDirectory": "dist/spa",
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
